# Release notes

The root `CHANGELOG.md` remains the canonical release history. This page shows only the 10 most recent version sections to keep the docs page readable.

```{include} ../_generated/recent-changelog.md
```
