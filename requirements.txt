# IvoryOS Core Requriements
bcrypt
Flask[async]
Flask-Login
Flask-Session
Flask-SocketIO
Flask-SQLAlchemy
Flask-WTF
SQLAlchemy-Utils
pandas

# pyproject.toml is the source of truth for package metadata and optional
# dependency groups. This installs the core IvoryOS package.


# Optional groups are installed through extras, for example:
#   pip install ".[dev]"                 # local test/development install
#   pip install ".[llm]"                 # in-app text-to-workflow design agent
#   pip install ".[db]"                  # PostgreSQL support
#   pip install ".[optimizer-ax]"        # Ax optimizer
#   pip install ".[optimizer-baybe]"     # BayBE optimizer
#   pip install ".[optimizer-nimo]"      # NIMO optimizer
#   pip install ".[optimizers]"          # all optimizer adapters

